document.addEventListener('DOMContentLoaded',()=>{const setup=(slides,dots)=>{if(!slides.length)return;let current=0;const show=index=>{current=(index+slides.length)%slides.length;const slide=slides[current];if(slide?.dataset.background&&!slide.style.backgroundImage)slide.style.backgroundImage=`url('${slide.dataset.background}')`;slides.forEach((item,i)=>item.classList.toggle('active',i===current));dots.forEach((dot,i)=>dot.classList.toggle('active',i===current))};dots.forEach((dot,i)=>dot.addEventListener('click',()=>show(i)));setInterval(()=>show(current+1),5000)};setup([...document.querySelectorAll('.hero-bg')],[...document.querySelectorAll('.slider-dots button')]);document.querySelectorAll('[data-collection-slider]').forEach(banner=>setup([...banner.querySelectorAll('.women-promo-slide')],[...banner.querySelectorAll('.women-promo-dots button')]))});
